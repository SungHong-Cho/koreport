.row.page-header
  .span2.offset2.title 마이 페이지
  .span2.offset6.email= current_user.email if user_signed_in?

- content_for :below do
  .row
    .span2
      %ul.nav.nav-pills.nav-stacked
        %li
          = link_to '구매내역 관리', '#', class: 'active'
        %li
          = link_to '비밀번호 변경', '#'
    .span10
      .window
        .row.window-bar
          .span3.window-label
            %span 구매내역 관리
          .span7
            %span 홈 > 마이 페이지 > 구매내역 관리
        .row.window-content
          .span10
            = render 'shared/report_list'

/
  .pakage.purchase
    .table-head
      .row
        .span8
          %h3 title
        .span1
          %h3 page
        .span1
          %h3 preview
        .span2
          %h3 price
    .table-body
      - @reports.each_with_index do |report, index|
        .row
          .span6
            %p= report.title
          .span1
            %p= report.page
          .span1
            %button.btn.btn-danger{'data-toggle' => 'collapse', 'data-target' => "#content#{index}"} preview
          .span2
            %p= "#{report.price} WON"
        .row
          .span12.collapse{ :id => "content#{index}"}= report.content


  